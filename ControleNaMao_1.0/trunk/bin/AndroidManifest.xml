<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="controle.mao"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="8" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Light.NoTitleBar" >
        <activity
            android:name=".ControleNaMaoActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Light.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TelaListaCartoes"
            android:label="Cartões"
            android:theme="@android:style/Theme.Light.NoTitleBar" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="controle.mao.ControleNaMaoActivity" />
        </activity>
        <activity android:name=".TelaAddCartoes" android:label="Adicionar Novo Cartão">
            <meta-data android:name="android.support.PARENT_ACTIVITY" android:value="controle.mao.ControleNaMaoActivity" />
        </activity><activity
            android:name=".TelaListaCategorias"
            android:label="Cartegorias" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="controle.mao.ControleNaMaoActivity" />
        </activity>
        
    </application>

</manifest>